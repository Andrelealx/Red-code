# TicketPrime - Engine de Ingressos

### 🛠️ Como Executar o Projeto
Para rodar a API localmente, siga os comandos abaixo no terminal:

```powershell
# 1. Entre na pasta do código-fonte
cd src/TicketPrimeApi

# 2. Execute a aplicação
dotnet run